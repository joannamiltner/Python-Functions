# # Given a list, write a function that charges all positive numbers in the list to "bit". Example: biggie_size([-1,3,5,-5])returns that same list, but whose values are now [-1, "big", "big", -5]
# def biggie_size(arr):
#     for i in range(len(arr)):
#         if arr[i] > 0:
#             arr[i]="big"
#     print(arr)
# biggie_size([-1,3,5,-5])

# def bigger_sizeaddar(arr):
#     newArr=[]
#     for i in range(len(arr)):
#         if arr[i] < 0:
#             newArr.append(arr[i])
#         else:
#             newArr.append("big")
#     print (newArr)
# # biggie_sizeaddar([-1,3,5,-5])


# Given a list of numbers, create a function to replace the last value with the number of positive values. (Note that zero is not considered to be a positive number) Example: count_positve ([-1,1,1,1,1]) changes the list to [-1,1,1,3] and returns it
def 

